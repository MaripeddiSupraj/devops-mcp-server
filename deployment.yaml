apiVersion: v1
kind: ServiceAccount
metadata:
  name: devops-mcp-sa
  labels:
    app: devops-mcp
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: devops-mcp-readonly
rules:
- apiGroups: [""]
  resources: ["pods", "pods/log", "events", "services"]
  verbs: ["get", "list", "watch"]
- apiGroups: ["apps"]
  resources: ["deployments"]
  verbs: ["get", "list", "watch"]
- apiGroups: ["networking.k8s.io"]
  resources: ["ingresses"]
  verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: devops-mcp-readonly-binding
subjects:
- kind: ServiceAccount
  name: devops-mcp-sa
  namespace: default
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: devops-mcp-readonly
---
apiVersion: v1
kind: Secret
metadata:
  name: devops-mcp-secrets
type: Opaque
stringData:
  # Comma-separated static bearer tokens for MCP auth.
  MCP_AUTH_TOKENS: "replace-with-strong-token"
  # Shared secret used to validate short-lived terraform apply approval tokens.
  TERRAFORM_APPLY_APPROVAL_SECRET: "replace-with-strong-secret"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: devops-mcp
  labels:
    app: devops-mcp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: devops-mcp
  template:
    metadata:
      labels:
        app: devops-mcp
    spec:
      serviceAccountName: devops-mcp-sa
      containers:
      - name: mcp
        image: devops-mcp:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
          name: sse
        env:
        - name: MCP_TRANSPORT
          value: "sse"
        - name: MCP_AUTH_ENABLED
          value: "false"
        - name: MCP_AUTH_TOKENS
          valueFrom:
            secretKeyRef:
              name: devops-mcp-secrets
              key: MCP_AUTH_TOKENS
        - name: TERRAFORM_ALLOW_UNRESTRICTED
          value: "false"
        - name: TERRAFORM_APPLY_REQUIRE_APPROVAL
          value: "true"
        - name: TERRAFORM_APPLY_TOKEN_TTL_SECONDS
          value: "300"
        - name: TERRAFORM_APPLY_APPROVAL_SECRET
          valueFrom:
            secretKeyRef:
              name: devops-mcp-secrets
              key: TERRAFORM_APPLY_APPROVAL_SECRET
        # Update to one or more comma-separated safe IaC roots in your runtime.
        - name: TERRAFORM_ALLOWED_ROOTS
          value: "/workspace/infra"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          capabilities:
            drop: ["ALL"]
---
apiVersion: v1
kind: Service
metadata:
  name: devops-mcp
spec:
  selector:
    app: devops-mcp
  ports:
  - port: 80
    targetPort: 8000
    name: http
